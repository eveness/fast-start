Быстрый старт с Browsersync для локальных тестов с простым отслеживанием файлов html, js и css в папке `public`.

```
git clone git@github.com:eveness/fast-start.git .
```
```
npm install
``` 
````
npm start
```